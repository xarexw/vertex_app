from django.urls import path
from .views import subscription_list

urlpatterns = [
    # Ось це ім'я 'subscription_list' шукає твій Dashboard
    path('', subscription_list, name='subscription_list'),
]